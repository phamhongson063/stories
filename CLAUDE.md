# Quy tắc dự án stories

- **Claude không tự ý `git commit` / `git push`.** Sau khi sửa file xong, để nguyên thay đổi trong working tree, user sẽ tự commit và push khi cần.
- **Khi dịch, loại bỏ mọi ký tự markup / dấu phân cách trang trí** khỏi bản dịch đầu ra: `***`, `~~~`, `---`, `___`, `**`, `*`, `~`, kể cả khi có khoảng trắng giữa các dấu (`* * *`, `- - -`) hay chỉ một dấu đơn lẻ. Tiểu thuyết chỉ giữ nội dung văn bản thuần.
